<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Browse Products - FarmerMarketPlace</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #2196F3, #1976D2);
            min-height: 100vh;
            color: #333;
        }

        .navbar {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0,0,0,0.1);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: #2196F3;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
        }

        .nav-links a {
            text-decoration: none;
            color: #333;
            font-weight: 500;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: #2196F3;
        }

        .container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 2rem;
        }

        .header {
            background: white;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 25px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            text-align: center;
        }

        .filters {
            background: white;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 25px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        .filter-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }

        .filter-group {
            display: flex;
            flex-direction: column;
        }

        .filter-group label {
            margin-bottom: 5px;
            font-weight: 600;
            color: #333;
        }

        .filter-group select,
        .filter-group input {
            padding: 10px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .product-card {
            background: white;
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }

        .product-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 15px;
        }

        .product-name {
            font-size: 1.3rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }

        .product-category {
            background: #e3f2fd;
            color: #1976d2;
            padding: 4px 12px;
            border-radius: 15px;
            font-size: 0.8rem;
            text-transform: capitalize;
        }

        .product-price {
            font-size: 2rem;
            font-weight: bold;
            color: #2196F3;
            margin-bottom: 10px;
        }
        
        .negotiation-price {
            font-size: 1rem;
            font-weight: normal;
            margin-top: 5px;
            padding: 4px 8px;
            border-radius: 12px;
            color: white;
            text-align: center;
        }
        
        .negotiation-open {
            background: linear-gradient(135deg, #FF9800, #F57C00);
        }
        
        .negotiation-accepted {
            background: linear-gradient(135deg, #4CAF50, #388E3C);
        }
        
        .price-comparison {
            font-size: 0.9rem;
            margin-top: 3px;
            font-weight: 500;
        }
        
        .savings {
            color: #4CAF50;
        }
        
        .negotiating {
            color: #FF9800;
        }

        .product-details {
            margin-bottom: 15px;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            font-size: 0.9rem;
        }

        .detail-label {
            color: #666;
        }

        .detail-value {
            font-weight: 600;
            color: #333;
        }

        .product-description {
            color: #666;
            font-size: 0.9rem;
            line-height: 1.4;
            margin-bottom: 15px;
        }

        .farmer-info {
            background: #f5f5f5;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 15px;
        }

        .farmer-name {
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }

        .farmer-contact {
            color: #666;
            font-size: 0.9rem;
        }

        .product-actions {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 8px;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            text-align: center;
            flex: 1;
        }

        .btn-primary {
            background: #2196F3;
            color: white;
        }

        .btn-primary:hover {
            background: #1976D2;
        }

        .btn-outline {
            border: 2px solid #2196F3;
            color: #2196F3;
            background: transparent;
        }

        .btn-outline:hover {
            background: #2196F3;
            color: white;
        }

        .organic-badge {
            background: #4CAF50;
            color: white;
            padding: 4px 8px;
            border-radius: 10px;
            font-size: 0.7rem;
            position: absolute;
            top: 15px;
            right: 15px;
        }

        .no-products {
            text-align: center;
            padding: 50px;
            color: #666;
        }

        @media (max-width: 768px) {
            .container {
                padding: 0 1rem;
            }
            
            .filter-row {
                grid-template-columns: 1fr;
            }
            
            .products-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">🌾 FarmerMarketPlace - Marketplace</div>
            <div class="nav-links">
                <a href="/client-dashboard">Dashboard</a>
                <a href="/products">Browse</a>
                <a href="/orders/history">Orders</a>
                <a href="#negotiations">Negotiations</a>
                <a href="/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="header">
            <h1>🛒 Browse Fresh Products</h1>
            <p>Discover quality produce directly from local farmers</p>
        </div>

        <div class="filters">
            <form method="GET" action="/products">
                <div class="filter-row">
                    <div class="filter-group">
                        <label for="category">Category</label>
                        <select id="category" name="category">
                            <option value="all">All Categories</option>
                            <% categories.forEach(cat => { %>
                                <option value="<%= cat %>" <%= filters.category === cat ? 'selected' : '' %>>
                                    <%= cat.charAt(0).toUpperCase() + cat.slice(1) %>
                                </option>
                            <% }); %>
                        </select>
                    </div>

                    <div class="filter-group">
                        <label for="location">Location</label>
                        <input type="text" id="location" name="location" placeholder="Enter city or state"
                               value="<%= filters.location || '' %>">
                    </div>

                    <div class="filter-group">
                        <label for="minPrice">Min Price (₹)</label>
                        <input type="number" id="minPrice" name="minPrice" placeholder="0"
                               value="<%= filters.minPrice || '' %>">
                    </div>

                    <div class="filter-group">
                        <label for="maxPrice">Max Price (₹)</label>
                        <input type="number" id="maxPrice" name="maxPrice" placeholder="1000"
                               value="<%= filters.maxPrice || '' %>">
                    </div>
                </div>
                
                <div style="display: flex; gap: 15px; align-items: center;">
                    <label style="display: flex; align-items: center; gap: 8px;">
                        <input type="checkbox" name="organic" value="true" <%= filters.organic === 'true' ? 'checked' : '' %>>
                        Organic Products Only
                    </label>
                    <button type="submit" class="btn btn-primary" style="flex: none; padding: 10px 25px;">
                        🔍 Apply Filters
                    </button>
                </div>
            </form>
        </div>

        <div class="products-grid">
            <% if (products && products.length > 0) { %>
                <% products.forEach(product => { %>
                    <div class="product-card" style="position: relative;">
                        <% if (product.isOrganic) { %>
                            <div class="organic-badge">🌿 ORGANIC</div>
                        <% } %>

                        <div class="product-header">
                            <div>
                                <div class="product-name"><%= product.name %></div>
                                <div class="product-category"><%= product.category %></div>
                            </div>
                        </div>

                        <div class="product-price">
                            ₹<%= product.price.toLocaleString() %>/<%= product.unit %>
                            <div class="negotiation-price" id="negotiation-<%= product._id %>" style="display: none;"></div>
                        </div>

                        <div class="product-details">
                            <div class="detail-row">
                                <span class="detail-label">Available:</span>
                                <span class="detail-value"><%= product.quantity %> <%= product.unit %></span>
                            </div>
                            <div class="detail-row">
                                <span class="detail-label">Location:</span>
                                <span class="detail-value"><%= product.location %></span>
                            </div>
                            <% if (product.harvestDate) { %>
                                <div class="detail-row">
                                    <span class="detail-label">Harvested:</span>
                                    <span class="detail-value"><%= new Date(product.harvestDate).toLocaleDateString() %></span>
                                </div>
                            <% } %>
                        </div>

                        <div class="product-description">
                            <%= product.description.length > 100 ? 
                                product.description.substring(0, 100) + '...' : 
                                product.description %>
                        </div>

                        <div class="farmer-info">
                            <div class="farmer-name">👨‍🌾 <%= product.farmerName %></div>
                            <div class="farmer-contact">📞 <%= product.farmerContact %></div>
                        </div>

                        <div class="product-actions">
                            <a href="tel:<%= product.farmerContact %>" class="btn btn-outline">📞 Call</a>
                            <button onclick="negotiatePrice('<%= product._id %>', '<%= product.price %>', '<%= product.unit %>')" class="btn btn-outline">💬 Negotiate</button>
                            <button onclick="orderProduct('<%= product._id %>')" class="btn btn-primary">🛒 Order</button>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="no-products">
                    <h3>No products found</h3>
                    <p>Try adjusting your filters or search criteria</p>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        function orderProduct(productId) {
            // Redirect to order page
            const params = new URLSearchParams({
                productId: productId
            });
            window.location.href = '/place-order?' + params.toString();
        }
        
        function negotiatePrice(productId, currentPrice, unit) {
            // Check if there's already an active negotiation for this product
            <% if (userNegotiations && userNegotiations.length > 0) { %>
                const negotiations = <%- JSON.stringify(userNegotiations) %>;
                const existingNegotiation = negotiations.find(n => n.productId._id === productId && n.status === 'open');
                
                if (existingNegotiation) {
                    if (confirm('You already have an active negotiation for this product. Would you like to continue the existing chat?')) {
                        window.location.href = `/negotiations/${existingNegotiation._id}/chat`;
                        return;
                    } else {
                        return;
                    }
                }
            <% } %>
            
            // Show negotiation modal or redirect to negotiation page
            const quantity = prompt(`How many ${unit} would you like to negotiate for?`, '1');
            if (!quantity || quantity <= 0) return;
            
            const offeredPrice = prompt(`Current price is ₹${currentPrice} per ${unit}. What price would you like to offer?`, currentPrice);
            if (!offeredPrice) return;
            
            const message = prompt('Add a message (optional):');
            
            // Get current user ID (in real app, from session/auth)
            const buyerId = '<%= userId %>' || localStorage.getItem('userId') || 'demo-buyer-id';
            
            // Create negotiation
            fetch('/negotiations/create', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({
                    buyerId: buyerId,
                    productId: productId,
                    quantity: parseInt(quantity),
                    offeredPrice: parseFloat(offeredPrice),
                    message: message
                })
            })
            .then(response => response.json())
            .then(result => {
                if (result.success) {
                    alert('Negotiation started! Redirecting to chat...');
                    window.location.href = `/negotiations/${result.negotiationId}/chat`;
                } else {
                    alert('Failed to start negotiation: ' + (result.message || 'Unknown error'));
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Network error. Please try again.');
            });
        }
        
        // Display negotiation prices
        function displayNegotiationPrices() {
            <% if (userNegotiations && userNegotiations.length > 0) { %>
                const negotiations = <%- JSON.stringify(userNegotiations) %>;
                
                negotiations.forEach(negotiation => {
                    const productId = negotiation.productId._id;
                    const negotiationElement = document.getElementById(`negotiation-${productId}`);
                    
                    if (negotiationElement) {
                        const originalPrice = negotiation.productId.price;
                        const negotiatedPrice = negotiation.finalPrice || negotiation.initialPrice;
                        const status = negotiation.status;
                        
                        let html = '';
                        let className = '';
                        
                        if (status === 'open') {
                            className = 'negotiation-open';
                            html = `💬 Negotiating: ₹${negotiatedPrice}/${negotiation.unit}`;
                            
                            if (negotiatedPrice < originalPrice) {
                                const savings = originalPrice - negotiatedPrice;
                                html += `<div class="price-comparison negotiating">Potential savings: ₹${savings}/${negotiation.unit}</div>`;
                            }
                        } else if (status === 'accepted') {
                            className = 'negotiation-accepted';
                            html = `✅ Agreed: ₹${negotiatedPrice}/${negotiation.unit}`;
                            
                            if (negotiatedPrice < originalPrice) {
                                const savings = originalPrice - negotiatedPrice;
                                html += `<div class="price-comparison savings">You saved: ₹${savings}/${negotiation.unit}</div>`;
                            }
                        }
                        
                        negotiationElement.className = `negotiation-price ${className}`;
                        negotiationElement.innerHTML = html;
                        negotiationElement.style.display = 'block';
                        
                        // Add click handler to open negotiation chat
                        negotiationElement.style.cursor = 'pointer';
                        negotiationElement.onclick = function() {
                            window.location.href = `/negotiations/${negotiation._id}/chat?userId=<%= userId %>&userType=buyer`;
                        };
                    }
                });
            <% } %>
        }
        
        // Set demo user ID for testing
        if (!localStorage.getItem('userId')) {
            localStorage.setItem('userId', 'demo-buyer-' + Date.now());
        }
        
        // Call on page load
        document.addEventListener('DOMContentLoaded', function() {
            displayNegotiationPrices();
        });
        
        // Display negotiation prices
        function displayNegotiationPrices() {
            <% if (userNegotiations && userNegotiations.length > 0) { %>
                const negotiations = <%- JSON.stringify(userNegotiations) %>;
                
                negotiations.forEach(negotiation => {
                    const productId = negotiation.productId._id;
                    const negotiationElement = document.getElementById(`negotiation-${productId}`);
                    
                    if (negotiationElement) {
                        const originalPrice = negotiation.productId.price;
                        const negotiatedPrice = negotiation.finalPrice || negotiation.initialPrice;
                        const status = negotiation.status;
                        
                        let html = '';
                        let className = '';
                        
                        if (status === 'open') {
                            className = 'negotiation-open';
                            html = `💬 Negotiating: ₹${negotiatedPrice}/${negotiation.unit}`;
                            
                            if (negotiatedPrice < originalPrice) {
                                const savings = originalPrice - negotiatedPrice;
                                html += `<div class="price-comparison negotiating">Potential savings: ₹${savings}/${negotiation.unit}</div>`;
                            }
                        } else if (status === 'accepted') {
                            className = 'negotiation-accepted';
                            html = `✅ Agreed: ₹${negotiatedPrice}/${negotiation.unit}`;
                            
                            if (negotiatedPrice < originalPrice) {
                                const savings = originalPrice - negotiatedPrice;
                                html += `<div class="price-comparison savings">You saved: ₹${savings}/${negotiation.unit}</div>`;
                            }
                        }
                        
                        negotiationElement.className = `negotiation-price ${className}`;
                        negotiationElement.innerHTML = html;
                        negotiationElement.style.display = 'block';
                        
                        // Add click handler to open negotiation chat
                        negotiationElement.style.cursor = 'pointer';
                        negotiationElement.onclick = function() {
                            window.location.href = `/negotiations/${negotiation._id}/chat?userId=<%= userId %>&userType=buyer`;
                        };
                    }
                });
            <% } %>
        }
        
        // Call on page load
        document.addEventListener('DOMContentLoaded', function() {
            displayNegotiationPrices();
        });
    </script>
</body>
</html>    <!-- Negotiation Setup Modal -->
    <div class="negotiation-modal" id="negotiationModal">
        <div class="negotiation-modal-content">
            <div class="negotiation-modal-header">
                <h2 class="negotiation-modal-title">💬 Start Price Negotiation</h2>
                <button class="close-modal" onclick="closeNegotiationModal()">&times;</button>
            </div>
            
            <div class="negotiation-modal-body">
                <div class="success-message" id="negotiationSuccessMessage" style="display: none;"></div>
                <div class="error-message" id="negotiationErrorMessage" style="display: none;"></div>
                
                <!-- Product Preview -->
                <div class="product-preview">
                    <h3 id="negotiationProductName">Product Name</h3>
                    <div class="current-price-info">
                        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
                            <span>Current Price:</span>
                            <span style="font-weight: bold; font-size: 18px;" id="negotiationCurrentPrice">₹0</span>
                        </div>
                        <div style="display: flex; justify-content: space-between; align-items: center;">
                            <span>Category:</span>
                            <span id="negotiationProductCategory">Category</span>
                        </div>
                    </div>
                </div>

                <!-- Negotiation Form -->
                <div class="negotiation-form">
                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label" for="negotiationQuantity">Quantity Needed</label>
                            <input type="number" class="form-input" id="negotiationQuantity" 
                                   placeholder="Enter quantity" min="1" value="1" required>
                            <small style="color: #666; font-size: 12px;">Available: <span id="negotiationAvailable">0</span> <span id="negotiationUnit">units</span></small>
                        </div>
                        <div class="form-group">
                            <label class="form-label" for="negotiationOfferedPrice">Your Offered Price (per unit)</label>
                            <input type="number" class="form-input" id="negotiationOfferedPrice" 
                                   placeholder="Enter your offer" step="0.01" min="0.01" required>
                        </div>
                    </div>
                    
                    <div class="form-group form-row-full">
                        <label class="form-label" for="negotiationMessage">Message to Farmer (Optional)</label>
                        <textarea class="form-input" id="negotiationMessage" rows="3" 
                                  placeholder="Hi! I'm interested in buying your product. Would you consider my price offer?"></textarea>
                    </div>
                    
                    <div class="price-comparison-preview" id="priceComparisonPreview" style="display: none;">
                        <span>Potential Total:</span>
                        <div>
                            <span id="negotiationTotalPreview">₹0</span>
                            <span class="savings-preview" id="savingsPreview"></span>
                        </div>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="negotiation-actions">
                    <button type="button" class="btn-cancel-negotiation" onclick="closeNegotiationModal()">Cancel</button>
                    <button type="button" class="btn-start-negotiation" id="startNegotiationBtn" onclick="submitNegotiation()">
                        <span id="negotiationBtnText">Start Chat & Negotiate</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
